% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recode.R
\name{recode_demographics}
\alias{recode_demographics}
\title{Recode Demographic Variables}
\usage{
recode_demographics(
  x,
  sped_input_var = "disability_status",
  sped_output_var = NULL,
  el_input_var = "el_overall_proficiency_level",
  el_output_var = NULL,
  race_input_var = "race",
  ethnicity_input_var = "ethnicity",
  race_output_var = NULL
)
}
\arguments{
\item{x}{A data frame containing the demographic variables to be recoded. This should be the result of \code{\link[=ingest_student_data_extract]{ingest_student_data_extract()}}}

\item{sped_input_var}{Character. The name of the column containing SPED status codes.
Defaults to "disability_status".}

\item{sped_output_var}{Character (optional). The name of the column to store the recoded
SPED status. If \code{NULL} (the default), the recoded values will overwrite the input column.}

\item{el_input_var}{Character. The name of the column containing EL proficiency level codes.
Defaults to "el_overall_proficiency_level".}

\item{el_output_var}{Character (optional). The name of the column to store the recoded
EL status. If \code{NULL} (the default), the recoded values will overwrite the input column.}

\item{race_input_var}{Character. The name of the column containing race codes.
Defaults to "race".}

\item{ethnicity_input_var}{Character. The name of the column containing ethnicity codes.
Defaults to "ethnicity".}

\item{race_output_var}{Character (optional). The name of the column to store the recoded
race/ethnicity. If \code{NULL} (the default), the recoded values will overwrite the input column.}
}
\value{
A data frame with the specified demographic variables recoded.
}
\description{
Recodes demographic variables (SPED status, EL status, race/ethnicity) in a data frame,
allowing for custom input and output column names.
}
\examples{
\dontrun{
# Assuming you have a data frame called 'student_data'

# Recode with default column names:
student_data <- recode_demographics(student_data)

# Recode with custom output column names:
student_data <- recode_demographics(
    student_data,
    sped_output_var = "sped_status_text",
    el_output_var = "el_status_text",
    race_output_var = "race_ethnicity_text"
)

# Recode with different input column names:
student_data <- recode_demographics(
    student_data,
    sped_input_var = "special_ed_code",
    el_input_var = "el_prof_level",
    race_input_var = "race_code",
    ethnicity_input_var = "hispanic_flag",
    sped_output_var = "sped_status_text",
    el_output_var = "el_status_text",
    race_output_var = "race_ethnicity_text"
)
}
}
