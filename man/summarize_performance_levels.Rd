% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pass_rates.R
\name{summarize_performance_levels}
\alias{summarize_performance_levels}
\title{Summarize Performance Levels}
\usage{
summarize_performance_levels(
  x,
  group_vars = NULL,
  drop_parent_requested = TRUE,
  drop_failing_retests = TRUE,
  convert_performance_levels = TRUE
)
}
\arguments{
\item{x}{A data frame containing student data, ideally from \code{\link[=ingest_student_data_extract]{ingest_student_data_extract()}}.}

\item{group_vars}{NULL or a character vector specifying the column names to group the data by (e.g., "school_name", "test_name"). If \code{NULL} (the default), no grouping will occur.}

\item{drop_parent_requested}{Logical. If \code{TRUE} (the default), exclude tests marked as parent-requested.}

\item{drop_failing_retests}{Logical. If \code{TRUE} (the default), exclude failing retests.}

\item{convert_performance_levels}{Logical. If \code{TRUE} (the default), convert numeric performance level codes to text labels using \code{\link[=recode_performance_levels]{recode_performance_levels()}}.}
}
\value{
A data frame summarizing performance levels.  It includes the grouping variables,
\code{performance_level} (either numeric or text), \code{n_performance_level} (the count of students
at that level within the group), \code{n_total} (the total number of students in the group),
and \code{pct} (the percentage of students at that level within the group).
}
\description{
Summarizes the distribution of performance levels within groups in a student data extract.
Calculates the count and percentage of students at each performance level for each group.
}
\examples{
\dontrun{
# Example usage with a hypothetical 'student_data' data frame:
summary_df <- summarize_performance_levels(
    student_data,
    group_vars = c("school_name", "test_name")
)

# Example with numeric performance levels:
summary_df_numeric <- summarize_performance_levels(
    student_data,
    group_vars = "school_name",
    convert_performance_levels = FALSE
)
}
}
